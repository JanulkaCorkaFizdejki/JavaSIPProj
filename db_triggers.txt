1)

CREATE TRIGGER insert_into_conn_list
         AFTER INSERT
            ON connect_list
BEGIN
    INSERT INTO connect_time (
                                 id_connect_list,
                                 conn_time
                             )
                             VALUES (
                                 MAX(connect_list.id),
                                 0
                             );
END;
